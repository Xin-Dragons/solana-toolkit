#!/usr/bin/env node

const files = process.argv.slice(2);

if (files.length !== 1) {
  console.error('Usage: get-owners path/to/list/of/addresses.json')
  process.exit(-1)
}

const input = files[0];



async function getOwners() {
  const owners = await getNftOwners(json);


}

getOwners();
